# Revisions that are pure reformatting and should be skipped when working out
# who last touched a line.
#
# Use it for a single command:
#     git blame --ignore-revs-file .git-blame-ignore-revs <file>
#
# Or configure it once, permanently:
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub reads this file automatically.

# STYLE: Apply clang-format to the whole tree
1b30afa035afe507062da2b7bfa7bd2a0e2debe9
